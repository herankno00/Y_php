# 如何修改配置停止localhost跳转

## 问题确认

✅ **已确认**：清除缓存后不跳转了  
❌ **问题**：运行IIS后又开始跳转

这说明是**PHP代码中的强制重定向配置**导致的问题。

---

## 修复方法

### 方法一：修改数据库配置（推荐）

PbootCMS的配置存储在SQLite数据库中。

**步骤：**

1. **登录后台管理系统**
   ```
   访问：http://localhost/guanli.php
   使用管理员账号登录
   ```

2. **进入系统配置**
   ```
   后台 → 全局配置 → 配置参数
   ```

3. **修改以下配置项**
   - **开启https** → 设置为 `关闭` 或 `0`
   - **主域名** → 留空或删除
   - **主域名跳转** → 设置为 `关闭` 或 `0`
   - **手机域名** → 留空（如果不需要）

4. **保存配置**

5. **清除缓存**
   ```
   后台 → 内容管理 → 清理缓存
   ```

6. **重新访问localhost**

---

### 方法二：直接修改数据库（如果无法登录后台）

**步骤：**

1. **下载SQLite数据库管理工具**
   - DB Browser for SQLite: https://sqlitebrowser.org/
   - 或使用其他SQLite工具

2. **打开数据库文件**
   ```
   文件位置：Y_php/data@918/dad193db2b4462332cef880da2149b07.db
   ```

3. **找到配置表**
   ```
   表名通常是：ay_config 或 pboot_config
   ```

4. **修改以下配置项**
   
   执行SQL语句：
   ```sql
   -- 关闭强制HTTPS
   UPDATE ay_config SET value = '0' WHERE name = 'to_https';
   
   -- 关闭主域名跳转
   UPDATE ay_config SET value = '0' WHERE name = 'to_main_domain';
   
   -- 清空主域名
   UPDATE ay_config SET value = '' WHERE name = 'main_domain';
   
   -- 清空手机域名（可选）
   UPDATE ay_config SET value = '' WHERE name = 'wap_domain';
   ```

5. **保存数据库**

6. **重启IIS**

7. **测试访问localhost**

---

### 方法三：直接修改PHP代码（临时方案，不推荐）

**⚠️ 注意**：这种方法会修改源代码，升级系统时可能丢失修改。仅用于紧急情况。

**步骤：**

1. **打开文件**
   ```
   Y_php/apps/common/HomeController.php
   ```

2. **找到第32-35行**（强制HTTPS重定向）
   ```php
   // 自动跳转HTTPS
   if (! is_https() && ! ! $tohttps = Config::get('to_https')) {
       header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
   }
   ```

3. **修改为**（添加localhost检测）
   ```php
   // 自动跳转HTTPS（跳过localhost）
   if (! is_https() && ! ! $tohttps = Config::get('to_https')) {
       // 检查是否为本地开发环境
       $host = $_SERVER['HTTP_HOST'];
       if ($host != 'localhost' && 
           $host != '127.0.0.1' && 
           !preg_match('/^localhost:/i', $host) &&
           !preg_match('/^127\.0\.0\.1:/i', $host)) {
           header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
       }
   }
   ```

4. **找到第37-47行**（强制主域名跳转）
   ```php
   // 自动跳转主域名
   if (! ($this->config('wap_domain') && is_mobile()) && (! ! $main_domain = Config::get('main_domain')) && (! ! $to_main_domain = Config::get('to_main_domain'))) {
       if (! preg_match('{^' . $main_domain . '$}i', get_http_host(true))) {
           if (is_https()) {
               header("Location: https://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
           } else {
               header("Location: http://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
           }
           exit();
       }
   }
   ```

5. **修改为**（添加localhost检测）
   ```php
   // 自动跳转主域名（跳过localhost）
   if (! ($this->config('wap_domain') && is_mobile()) && (! ! $main_domain = Config::get('main_domain')) && (! ! $to_main_domain = Config::get('to_main_domain'))) {
       $host = get_http_host(true);
       // 检查是否为本地开发环境
       if ($host != 'localhost' && 
           $host != '127.0.0.1' && 
           !preg_match('/^localhost$/i', $host) &&
           !preg_match('/^127\.0\.0\.1$/i', $host)) {
           if (! preg_match('{^' . $main_domain . '$}i', $host)) {
               if (is_https()) {
                   header("Location: https://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
               } else {
                   header("Location: http://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
               }
               exit();
           }
       }
   }
   ```

6. **保存文件**

7. **重启IIS**

8. **测试访问localhost**

---

## 推荐方案对比

| 方案 | 优点 | 缺点 | 推荐指数 |
|------|------|------|----------|
| 方法一：后台配置 | 简单、安全、不修改代码 | 需要能登录后台 | ⭐⭐⭐⭐⭐ |
| 方法二：直接修改数据库 | 不需要登录、快速 | 需要数据库工具 | ⭐⭐⭐⭐ |
| 方法三：修改PHP代码 | 可以精细控制 | 升级会丢失、不推荐 | ⭐⭐ |

---

## 验证修复是否成功

执行以下测试确认问题已解决：

### 测试1：基本访问
```
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 访问：http://localhost
3. 应该正常显示网站，不跳转
```

### 测试2：HTTPS访问
```
1. 访问：http://localhost
2. 检查地址栏，应该保持 http://localhost
3. 不应该自动跳转到 https://localhost
```

### 测试3：不同端口
```
如果使用非80端口（如8080）：
1. 访问：http://localhost:8080
2. 应该正常显示，不跳转
```

### 测试4：刷新缓存
```
1. 按 Ctrl+F5 强制刷新
2. 仍然应该正常显示，不跳转
```

---

## 如果修改后仍然跳转

### 检查清单

- [ ] 配置是否已保存？（检查数据库）
- [ ] IIS是否已重启？
- [ ] 浏览器缓存是否已清除？
- [ ] 是否有其他配置文件覆盖？

### 深度排查

1. **检查运行时配置缓存**
   ```
   删除目录：Y_php/runtime/config/
   重启IIS
   ```

2. **检查是否有多个配置文件**
   ```
   检查：Y_php/config/config.php
   看是否有 to_https 或 main_domain 的配置
   ```

3. **查看实际生效的配置**
   
   创建测试文件 `test_config.php`：
   ```php
   <?php
   define('IS_INDEX', true);
   require dirname(__FILE__) . '/core/start.php';
   
   echo "to_https: " . \core\basic\Config::get('to_https') . "<br>";
   echo "main_domain: " . \core\basic\Config::get('main_domain') . "<br>";
   echo "to_main_domain: " . \core\basic\Config::get('to_main_domain') . "<br>";
   ```
   
   访问：`http://localhost/test_config.php`
   
   查看输出的配置值是否为0或空。

4. **检查IIS重写规则**
   ```
   打开IIS管理器
   选择您的网站
   查看"URL重写"模块
   确认没有额外的重定向规则
   ```

---

## 生产环境配置建议

如果您需要在生产环境启用HTTPS和主域名跳转，建议：

### 使用环境判断

修改 `apps/common/HomeController.php`，在重定向代码前添加：

```php
// 定义开发环境检测函数
function is_local_env() {
    $host = $_SERVER['HTTP_HOST'];
    // 检查是否为本地环境
    return ($host == 'localhost' || 
            $host == '127.0.0.1' || 
            preg_match('/^localhost:/i', $host) ||
            preg_match('/^127\.0\.0\.1:/i', $host) ||
            preg_match('/\.local$/i', $host));
}

// 自动跳转HTTPS（仅非本地环境）
if (! is_local_env() && ! is_https() && ! ! $tohttps = Config::get('to_https')) {
    header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
}

// 自动跳转主域名（仅非本地环境）
if (! is_local_env() && ...) {
    // 原来的主域名跳转代码
}
```

这样可以：
- ✅ 本地开发时不跳转
- ✅ 生产环境正常跳转
- ✅ 不需要改配置

---

## 常见问题

### Q1：修改配置后还是跳转？
**A**：清除浏览器缓存和HSTS缓存（chrome://net-internals/#hsts）

### Q2：可以用IP访问吗（如192.168.1.100）？
**A**：如果启用了主域名跳转，IP访问也会被重定向。建议关闭主域名跳转或添加IP白名单。

### Q3：修改代码后升级系统会丢失吗？
**A**：会丢失。建议使用方法一（后台配置）或方法二（修改数据库）。

### Q4：能不能只在本地禁用，生产环境启用？
**A**：可以，使用上面"生产环境配置建议"中的代码。

### Q5：如何彻底禁用重定向功能？
**A**：在数据库中将相关配置都设为0或空即可。

---

## 技术原理说明

### 为什么清除缓存就好了？

1. PHP代码发送了 `301 Moved Permanently` 响应
2. 浏览器接收到301后**永久缓存**这个重定向规则
3. 即使服务器关闭，浏览器仍使用缓存的规则
4. 清除缓存后，浏览器重新请求服务器
5. 如果服务器关闭，显示连接失败
6. 如果服务器运行但有重定向配置，又会收到301

### 为什么运行IIS又跳转？

因为PHP代码中的重定向逻辑仍然在执行：

```php
// 这段代码在每次访问时都会执行
if (! is_https() && Config::get('to_https')) {
    header("Location: https://...", true, 301);  // 发送301重定向
}
```

只要配置中 `to_https` 不为0，每次访问都会触发重定向。

### 301 vs 302 的区别

- **301 Moved Permanently**：永久重定向，浏览器会缓存
- **302 Found**：临时重定向，浏览器不缓存

代码中使用的是301，所以会被浏览器永久缓存。

---

## 总结

**最简单的解决方案**：

1. 登录后台管理系统（http://localhost/guanli.php）
2. 进入"全局配置"
3. 关闭"开启HTTPS"和"主域名跳转"
4. 保存配置
5. 清理缓存
6. 重启IIS
7. 清除浏览器缓存
8. 测试访问

**如果无法登录后台**：

使用SQLite工具修改数据库，执行：
```sql
UPDATE ay_config SET value = '0' WHERE name = 'to_https';
UPDATE ay_config SET value = '0' WHERE name = 'to_main_domain';
```

**问题解决！** 🎉
