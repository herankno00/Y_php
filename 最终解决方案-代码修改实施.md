# 最终解决方案 - 代码修改实施指南

## 用户确认

用户已确认使用方法三（修改PHP代码）来解决localhost劫持问题。

---

## 需要修改的文件

**文件位置**：`/home/runner/work/Y_php/Y_php/apps/common/HomeController.php`（main分支）

**注意**：此文件在main分支，当前PR分支无法直接修改。用户需要在本地环境中应用以下修改。

---

## 具体修改内容

### 修改1：添加本地环境检测函数（在类中添加私有方法）

在 `HomeController` 类中添加一个检测本地环境的辅助方法：

```php
/**
 * 检测是否为本地开发环境
 * @return bool
 */
private function isLocalEnvironment()
{
    $host = $_SERVER['HTTP_HOST'];
    return (
        $host == 'localhost' || 
        $host == '127.0.0.1' || 
        strpos($host, 'localhost:') === 0 ||
        strpos($host, '127.0.0.1:') === 0 ||
        preg_match('/\.local$/i', $host) ||
        preg_match('/\.test$/i', $host)
    );
}
```

**添加位置**：在 `__construct()` 方法之后，其他方法之前。

---

### 修改2：HTTPS重定向（第32-35行附近）

**原代码：**
```php
// 自动跳转HTTPS
if (! is_https() && ! ! $tohttps = Config::get('to_https')) {
    //header("Location: http://" . $_SERVER['HTTP_HOST'], true, 301);
    header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
}
```

**修改为：**
```php
// 自动跳转HTTPS（跳过本地环境）
if (! is_https() && ! ! $tohttps = Config::get('to_https')) {
    if (!$this->isLocalEnvironment()) {
        //header("Location: http://" . $_SERVER['HTTP_HOST'], true, 301);
        header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
    }
}
```

---

### 修改3：主域名跳转（第37-47行附近）

**原代码：**
```php
// 自动跳转主域名
if (! ($this->config('wap_domain') && is_mobile()) && (! ! $main_domain = Config::get('main_domain')) && (! ! $to_main_domain = Config::get('to_main_domain'))) {
    if (! preg_match('{^' . $main_domain . '$}i', get_http_host(true))) {
        if (is_https()) {
            header("Location: https://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
        } else {
            header("Location: http://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
        }
        exit();
    }
}
```

**修改为：**
```php
// 自动跳转主域名（跳过本地环境）
if (! ($this->config('wap_domain') && is_mobile()) && (! ! $main_domain = Config::get('main_domain')) && (! ! $to_main_domain = Config::get('to_main_domain'))) {
    if (!$this->isLocalEnvironment() && ! preg_match('{^' . $main_domain . '$}i', get_http_host(true))) {
        if (is_https()) {
            header("Location: https://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
        } else {
            header("Location: http://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
        }
        exit();
    }
}
```

---

### 修改4：手机域名跳转（第95-101行附近）

**原代码：**
```php
} elseif (is_mobile() && $this->config('wap_domain') && $this->config('wap_domain') != get_http_host()) {
    if (is_https()) {
        $pre = 'https://';
    } else {
        $pre = 'http://';
    }
    header('Location:' . $pre . $this->config('wap_domain') . URL, true, 302);
```

**修改为：**
```php
} elseif (!$this->isLocalEnvironment() && is_mobile() && $this->config('wap_domain') && $this->config('wap_domain') != get_http_host()) {
    if (is_https()) {
        $pre = 'https://';
    } else {
        $pre = 'http://';
    }
    header('Location:' . $pre . $this->config('wap_domain') . URL, true, 302);
```

---

## 实施步骤

### 1. 切换到main分支并备份

```bash
cd /path/to/Y_php
git checkout main
cp apps/common/HomeController.php apps/common/HomeController.php.backup
```

### 2. 编辑文件

使用文本编辑器打开：
```
apps/common/HomeController.php
```

### 3. 应用四处修改

按照上面的说明：
1. 添加 `isLocalEnvironment()` 方法
2. 修改HTTPS重定向代码
3. 修改主域名跳转代码
4. 修改手机域名跳转代码

### 4. 保存并重启IIS

```bash
# 保存文件后重启IIS
iisreset
```

### 5. 清除浏览器缓存

按 `Ctrl+Shift+Delete` 清除浏览器缓存。

### 6. 测试

访问 `http://localhost`，验证：
- ✅ 不再跳转到HTTPS
- ✅ 不再跳转到其他域名
- ✅ 正常显示网站内容

---

## 预期效果

修改后：
- ✅ **本地开发环境**（localhost, 127.0.0.1）可以正常访问，不会重定向
- ✅ **生产环境**仍然正常执行HTTPS和主域名重定向
- ✅ **支持多种本地域名**：localhost, 127.0.0.1, *.local, *.test

---

## 技术说明

### 为什么这样修改？

1. **添加统一的检测方法**：避免代码重复，便于维护
2. **最小化修改**：只在必要的地方添加判断，不改变原有逻辑
3. **保持生产环境功能**：非本地环境仍然正常工作

### 支持的本地域名

- `localhost`
- `127.0.0.1`
- `localhost:端口`（如 `localhost:8080`）
- `127.0.0.1:端口`（如 `127.0.0.1:8080`）
- `*.local`（如 `mysite.local`）
- `*.test`（如 `mysite.test`）

---

## 故障排除

### 修改后网站无法访问

1. 检查PHP语法错误
2. 恢复备份文件：
   ```bash
   cp apps/common/HomeController.php.backup apps/common/HomeController.php
   iisreset
   ```

### 仍然跳转

1. 确认文件已保存
2. 确认IIS已重启
3. 清除浏览器HSTS缓存：
   - 访问 `chrome://net-internals/#hsts`
   - 输入 `localhost`，点击 `Delete`
4. 使用隐私模式测试

---

## 系统升级注意事项

**重要提醒**：PbootCMS升级时会覆盖此文件！

### 升级前

```bash
# 备份修改的文件
cp apps/common/HomeController.php HomeController.php.custom
```

### 升级后

1. 使用文本对比工具对比备份文件和新文件
2. 重新应用四处修改
3. 测试功能

### 更好的方案

考虑使用方法一（后台配置）或方法二（修改数据库），不受升级影响。

---

## 完整修改后的代码示例

```php
public function __construct()
{
    // 自动缓存基础信息
    cache_config();
    
    // 从配置文件读取cmsname参数来设置系统名称
    define("CMSNAME", $this->config("cmsname") ?: 'PbootCMS');

    // 站点关闭检测
    if (! ! $close_site = Config::get('close_site')) {
        $close_site_note = Config::get('close_site_note');
        error($close_site_note ?: '本站维护中，请稍后再访问，带来不便，敬请谅解！');
    }

    // 自动跳转HTTPS（跳过本地环境）
    if (! is_https() && ! ! $tohttps = Config::get('to_https')) {
        if (!$this->isLocalEnvironment()) {
            //header("Location: http://" . $_SERVER['HTTP_HOST'], true, 301);
            header("Location: https://" . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'], true,301);
        }
    }

    // 自动跳转主域名（跳过本地环境）
    if (! ($this->config('wap_domain') && is_mobile()) && (! ! $main_domain = Config::get('main_domain')) && (! ! $to_main_domain = Config::get('to_main_domain'))) {
        if (!$this->isLocalEnvironment() && ! preg_match('{^' . $main_domain . '$}i', get_http_host(true))) {
            if (is_https()) {
                header("Location: https://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
            } else {
                header("Location: http://" . $main_domain . ':' . $_SERVER['SERVER_PORT'], true, 301);
            }
            exit();
        }
    }
    
    // ... 其他代码保持不变 ...
    
    // 手机自适应主题（跳过本地环境）
    if ($this->config('open_wap')) {
        if ($this->config('wap_domain') && $this->config('wap_domain') == get_http_host()) {
            $this->setTheme(get_theme() . '/wap');
        } elseif (!$this->isLocalEnvironment() && is_mobile() && $this->config('wap_domain') && $this->config('wap_domain') != get_http_host()) {
            if (is_https()) {
                $pre = 'https://';
            } else {
                $pre = 'http://';
            }
            header('Location:' . $pre . $this->config('wap_domain') . URL, true, 302);
        } elseif (is_mobile() && ! $this->config('wap_domain')) {
            $this->setTheme(get_theme() . '/wap');
        }
    }
    
    // ... 其他代码 ...
}

/**
 * 检测是否为本地开发环境
 * @return bool
 */
private function isLocalEnvironment()
{
    $host = $_SERVER['HTTP_HOST'];
    return (
        $host == 'localhost' || 
        $host == '127.0.0.1' || 
        strpos($host, 'localhost:') === 0 ||
        strpos($host, '127.0.0.1:') === 0 ||
        preg_match('/\.local$/i', $host) ||
        preg_match('/\.test$/i', $host)
    );
}
```

---

## 总结

这个修改方案：
- ✅ **最小化改动**：只添加了一个方法和四处if判断
- ✅ **向后兼容**：生产环境功能完全不受影响
- ✅ **易于维护**：逻辑清晰，便于理解
- ✅ **灵活扩展**：可以轻松添加更多本地域名

**修改位置总结**：
1. 添加 `isLocalEnvironment()` 私有方法
2. 第32-35行：HTTPS重定向添加判断
3. 第37-47行：主域名跳转添加判断
4. 第95-101行：手机域名跳转添加判断

**修改完成后请记得**：
1. 重启IIS
2. 清除浏览器缓存
3. 测试localhost访问
4. 备份修改后的文件（以备升级后恢复）
